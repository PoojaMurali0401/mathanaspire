apiVersion: v1
kind: Pod
metadata:
  name: demo-pod
  labels:
    app: demo
spec:
  containers:
  - name: demo-container
    image: busybox:latest
    command: ["sh", "-c", "while true; do echo Hello Kubernetes; sleep 3600; done"]
